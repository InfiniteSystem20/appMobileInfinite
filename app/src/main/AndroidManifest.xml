<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.infinitesystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InfiniteSystem">
        <activity android:name=".controlador.ListaClienteActivity"></activity>
        <activity android:name=".controlador.CadAgendaActivity" />
        <activity android:name=".controlador.CadAtividadeActivity" />
        <activity android:name=".controlador.CadServicoActivity" />
        <activity android:name=".controlador.CadConsultorActivity" />
        <activity android:name=".controlador.CadClienteActivity" />
        <activity android:name=".controlador.MenuCadActivity" />
        <activity android:name=".controlador.SobreActivity" />
        <activity android:name=".controlador.MainActivity" />
        <activity android:name=".controlador.LoginActivity" />
        <activity android:name=".controlador.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>